<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.suhail.clinicapp">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClinicApp"
        tools:targetApi="31">
        <activity android:name=".ui.activities.OtpCode"/>
        <activity android:name=".ui.activities.LoginScreen"/>
        <activity android:name=".ui.activities.DoctorDetailsScreen"/>
        <activity android:name=".ForgetPasswordScreen"/>
        <activity android:name=".ui.activities.CreateAccount"/>
        <activity
            android:name=".ui.activities.TypeUsersActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>